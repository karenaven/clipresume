<?php
defined('MOODLE_INTERNAL') || die();

$plugin->component = 'mod_clipresume';  // Nombre del plugin.
$plugin->version = 2024092500;                // Versión del plugin (AñoMesDíaHoraMinuto).
$plugin->requires = 2021051700;               // Versión mínima de Moodle requerida.
$plugin->maturity = MATURITY_STABLE;          // Estado del plugin.
$plugin->release = 'v1.1';                    // Versión del plugin.
